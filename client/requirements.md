## Packages
lucide-react | Standard icon set for clean minimal design
framer-motion | Smooth entry animations and micro-interactions

## Notes
- Using clean minimal design with high contrast black/white theme
- Expecting backend to implement GET /api/hello and GET/POST /api/messages
- Error states handled gracefully in the UI if endpoints are missing
